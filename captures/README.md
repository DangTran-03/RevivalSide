# Captures

Local packet captures are written here by `tools/watch-counterside-capture.ps1`.

This folder is ignored except for this README. Do not commit `*.pcapng`, logs, or extracted packet binaries.
